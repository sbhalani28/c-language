#include<stdio.h>
main()
{
    sum(10,2,3);
    sum(10,55,6);
}
void sum(int p,int q,int r)
{
    if(p>q && p>r)
    {printf("P is big");}

    else if(q.p && q>r)
    {if (printf("Q is big");)}

    else{ printf("R is big");}
}